To run:
    - from ~/VSCode_Projects_SPACE/e-pyilc
    - `python -m cnilc_pipeline.run test.yaml`

After running:
    - Check the comparison with compare_new_old.ipynb
    - I want all generated maps to look the same

Next:
    - Once all maps look the same, I want to extract more and more of the PyILC stuff away, culling it to simpler operations.