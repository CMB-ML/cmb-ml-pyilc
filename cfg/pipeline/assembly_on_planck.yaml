defaults:
  - assembly_pyilc_hm
  - assembly_planck_as_sim
  - pipe_model_analysis
  # Adjustments needed:
  - _self_

convert_obs:
  assets_out:
    obs_maps:
      dataset: ${planck_ds}

convert_hms:
  assets_out:
    obs_maps:
      dataset: ${planck_ds}

make_weights:
  assets_in:
    obs_maps:
      dataset: ${planck_ds}
  dir_name: NILC_I_Weights_Planck

apply_weights:
  assets_in:
    obs_maps:
      dataset: ${planck_ds}
    fg_maps:
      working: ${trained_wd}
  dir_name: NILC_J_Applied_Weights_Planck

make_pred_ps_with_debias:
  dir_name: Analysis_K_Power_Spectra_Planck
